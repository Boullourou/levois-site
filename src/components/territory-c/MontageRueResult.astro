<section class="mh-rue-result mh-frame mh-frame--cream" aria-labelledby="mh-rue-title">
  <div class="mh-stage mh-section-pad">
    <header class="mh-rue-head">
      <div>
        <h1 class="mh-display" id="mh-rue-title">Ce que les ventes autour de cette adresse permettent de lire.</h1>
        <p class="mh-lead">Un état de démonstration calculé avec les données DVF déjà intégrées au site. Il décrit un échantillon historique ; il n’est jamais présenté comme une estimation du bien.</p>
      </div>
      <div class="mh-rue-address">
        <span>Adresse confirmée</span>
        <strong>8 Rue de la Ravaudière<br />28300 Lèves</strong>
        <p>Exemple déjà utilisé par LEVOIS · adresse avec numéro</p>
      </div>
    </header>

    <dl class="mh-proof-strip mh-rue-proof">
      <div><dt>Type homogène</dt><dd>Maison</dd></div>
      <div><dt>Rayon retenu</dt><dd>1 km</dd></div>
      <div><dt>Échantillon</dt><dd>81 <small>ventes</small></dd></div>
      <div><dt>Période réelle</dt><dd>2021—2025</dd></div>
    </dl>

    <div class="mh-rue-reading">
      <div class="mh-rue-range" aria-labelledby="mh-range-title">
        <h2 class="mh-subtitle" id="mh-range-title">La moitié des prix au m² se situe entre 2&nbsp;069&nbsp;€ et 2&nbsp;785&nbsp;€.</h2>
        <div class="mh-rue-range-line" role="img" aria-label="Quartile bas 2 069 euros par mètre carré, médiane 2 388 euros, quartile haut 2 785 euros">
          <i></i><i></i><i></i>
          <span>2&nbsp;069&nbsp;€<small>quartile bas</small></span>
          <span>2&nbsp;388&nbsp;€<small>médiane</small></span>
          <span>2&nbsp;785&nbsp;€<small>quartile haut</small></span>
        </div>
        <p>Du 8 janvier 2021 au 19 décembre 2025, maisons avec surface connue, hors mutations multi-lots. Le rayon commence à 250 m et s’élargit seulement jusqu’au premier seuil contenant au moins 20 ventes comparables.</p>
      </div>

      <aside class="mh-rue-limit" aria-labelledby="mh-rue-limit-title">
        <h2 id="mh-rue-limit-title">Ce que cela ne permet pas de conclure</h2>
        <ul>
          <li>La valeur précise d’un bien aujourd’hui</li>
          <li>L’effet de son état, de ses travaux ou de sa parcelle</li>
          <li>La demande actuelle ou un délai de vente</li>
          <li>Le prix auquel il devrait être proposé</li>
        </ul>
        <p>Source : DVF, DGFiP · extraction locale LEVOIS 2021–2025. Certaines ventes peuvent être absentes de la base.</p>
      </aside>
    </div>

    <div class="mh-rue-actions">
      <a class="mh-button mh-button--dark" href="#vr-explorer">Explorer une autre adresse</a>
      <a class="mh-text-link" href="/methode">Comprendre la méthode</a>
    </div>
  </div>
</section>

<style>
  .mh-rue-head { display: grid; grid-template-columns: 1.2fr .8fr; gap: clamp(50px, 9vw, 140px); align-items: end; margin-bottom: 74px; }
  .mh-rue-head .mh-lead { margin-top: 34px; }
  .mh-rue-address { align-self: stretch; display: flex; flex-direction: column; justify-content: flex-end; padding: 30px; color: var(--mh-cream); background: var(--mh-plum); }
  .mh-rue-address span { margin-bottom: 48px; color: var(--mh-coral); font-size: 12px; font-weight: 700; }
  .mh-rue-address strong { font-family: var(--mh-serif); font-size: clamp(28px, 3.2vw, 44px); font-weight: 400; line-height: 1; }
  .mh-rue-address p { margin: 22px 0 0; color: var(--mh-cream-soft); font-size: 11px; line-height: 1.5; }
  .mh-rue-proof { margin-bottom: 84px; }
  .mh-rue-proof small { font: 600 12px/1 var(--mh-sans); }
  .mh-rue-reading { display: grid; grid-template-columns: 1.25fr .75fr; gap: clamp(48px, 8vw, 118px); }
  .mh-rue-range > p { max-width: 72ch; margin: 40px 0 0; font-size: 13px; line-height: 1.65; }
  .mh-rue-range-line { position: relative; min-height: 144px; display: grid; grid-template-columns: repeat(3, 1fr); align-items: end; margin-top: 58px; padding-top: 40px; border-top: 1px solid var(--mh-line-dark); }
  .mh-rue-range-line::before { position: absolute; top: 38px; right: 0; left: 0; height: 12px; background: var(--mh-coral); content: ''; }
  .mh-rue-range-line i { position: absolute; z-index: 1; top: 29px; width: 1px; height: 30px; background: var(--mh-night); }
  .mh-rue-range-line i:nth-child(1) { left: 0; }
  .mh-rue-range-line i:nth-child(2) { left: 50%; }
  .mh-rue-range-line i:nth-child(3) { right: 0; }
  .mh-rue-range-line span { display: grid; gap: 7px; font-family: var(--mh-serif); font-size: clamp(28px, 3vw, 42px); line-height: 1; }
  .mh-rue-range-line span:nth-of-type(2) { text-align: center; }
  .mh-rue-range-line span:nth-of-type(3) { text-align: right; }
  .mh-rue-range-line small { font: 500 11px/1.2 var(--mh-sans); }
  .mh-rue-limit { padding: 34px; color: var(--mh-cream); background: var(--mh-night); }
  .mh-rue-limit h2 { margin: 0 0 30px; font-family: var(--mh-serif); font-size: clamp(28px, 3vw, 42px); font-weight: 400; line-height: 1; }
  .mh-rue-limit ul { margin: 0; padding: 0; list-style: none; border-top: 1px solid var(--mh-line-light); }
  .mh-rue-limit li { padding: 13px 0; border-bottom: 1px solid var(--mh-line-light); font-size: 13px; line-height: 1.45; }
  .mh-rue-limit > p { margin: 30px 0 0; color: var(--mh-cream-soft); font-size: 11px; line-height: 1.55; }
  .mh-rue-actions { display: flex; flex-wrap: wrap; align-items: center; gap: 18px; margin-top: 54px; }
  :global(.mh-rue-explorer) { color: var(--mh-cream); background: var(--mh-night); }
  :global(.mh-rue-explorer > div) { color: var(--mh-night); background: var(--mh-cream); }
  :global(.mh-rue-explorer > summary) { min-height: 74px; display: flex; align-items: center; justify-content: space-between; gap: 20px; padding: 16px var(--mh-edge); border-top: 1px solid var(--mh-line-light); border-bottom: 1px solid var(--mh-line-light); color: var(--mh-cream); background: var(--mh-night); font-weight: 700; cursor: pointer; list-style: none; }
  :global(.mh-rue-explorer > summary::-webkit-details-marker) { display: none; }
  :global(.mh-rue-explorer > summary::after) { content: '+'; color: var(--mh-coral); font-size: 24px; font-weight: 400; }
  :global(.mh-rue-explorer[open] > summary::after) { content: '−'; }

  @media (max-width: 820px) {
    .mh-rue-head,
    .mh-rue-reading { grid-template-columns: 1fr; }
    .mh-rue-address span { margin-bottom: 28px; }
  }
  @media (max-width: 620px) {
    .mh-rue-result .mh-stage { padding-inline: 20px; }
    .mh-rue-range-line { min-height: 220px; display: flex; flex-direction: column; align-items: stretch; gap: 24px; padding-top: 74px; }
    .mh-rue-range-line::before { top: 38px; }
    .mh-rue-range-line span,
    .mh-rue-range-line span:nth-of-type(2),
    .mh-rue-range-line span:nth-of-type(3) { display: flex; align-items: baseline; justify-content: space-between; text-align: left; }
    .mh-rue-limit { padding: 28px 22px; }
    :global(.mh-rue-explorer > summary) { padding-inline: 20px; }
  }
</style>
